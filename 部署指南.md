# 微信服务号连接器部署指南

## 版本信息

- **版本**: v2.0
- **描述**: 同步微信服务号用户、素材和消息数据，支持多账号管理和实时 Webhook 事件订阅
- **分类**: Messaging

## 环境要求

### 1. 基础环境

- **Java**: JDK 17
- **Maven**: 3.8+
- **PostgreSQL**: 15+
- **Nacos**: 2.5.1+
- **Docker**: 20.10+ (可选)
- **Docker Compose**: 2.0+ (可选)

### 2. 硬件要求

- **CPU**: 2 核心以上
- **内存**: 4GB 以上
- **磁盘**: 20GB 以上

## 部署步骤

### 1. 安装 PostgreSQL 数据库

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install postgresql-15

# CentOS/RHEL
sudo yum install postgresql15-server
sudo postgresql-setup initdb
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### 2. 创建数据库和用户

```bash
# 切换到 postgres 用户
sudo -u postgres psql

# 创建数据库
CREATE DATABASE livepulse_wechat_mp;

# 创建用户
CREATE USER wechat_user WITH PASSWORD 'your_password';

# 授权
GRANT ALL PRIVILEGES ON DATABASE livepulse_wechat_mp TO wechat_user;

# 退出
\q
```

### 3. 执行数据库脚本

```bash
# 进入 SQL 脚本目录
cd livepulse-connector-wechat-mp-open/docs/sql

# 执行建表脚本
psql -U wechat_user -d livepulse_wechat_mp -f wechat_mp_schema.sql
```

### 4. 配置 Nacos

#### 4.1 导入配置

在 Nacos 控制台创建以下配置：

**Data ID**: `postgresql-config.yml`
**Group**: `DEFAULT_GROUP`

```yaml
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgresql-host:5432/livepulse_wechat_mp
    username: wechat_user
    password: your_password
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      connection-timeout: 30000
      max-lifetime: 1200000

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.cs.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

**Data ID**: `kafka-config.yml`
**Group**: `DEFAULT_GROUP`

```yaml
spring:
  kafka:
    bootstrap-servers: kafka-host:9092
    consumer:
      group-id: wechat-mp-connector-group
      auto-offset-reset: latest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
```

### 5. 构建项目

```bash
# 进入项目根目录
cd livepluse-connectorx

# 编译打包
mvn clean package -DskipTests

# 查看生成的 JAR 文件
ls -lh livepulse-connector-wechat-mp-open/target/livepulse-connector-wechat-mp-open.jar
```

### 6. 配置应用

创建 `application-prod.yml` 文件：

```yaml
server:
  port: 23014

spring:
  application:
    name: wechat-mp-open-connector-server

# 微信服务号定时任务配置
wechat:
  mp:
    scheduled:
      enabled: true
      user-sync:
        cron: "0 0 */4 * * ?"  # 每4小时执行一次
      material-sync:
        cron: "0 0 2 * * ?"  # 每天凌晨2点
      message-sync:
        cron: "0 0 */6 * * ?"  # 每6小时执行一次
```

### 7. 启动应用

#### 方式一：直接启动

```bash
java -jar livepulse-connector-wechat-mp-open/target/livepulse-connector-wechat-mp-open.jar \
  --spring.profiles.active=prod \
  --NACOS_SERVER_ADDR=nacos-host:8848
```

#### 方式二：使用 Docker 启动

```bash
# 构建镜像
cd livepulse-connector-wechat-mp-open
docker build -t livepulse-connector-wechat-mp:2.0 .

# 启动容器
docker-compose up -d

# 查看日志
docker-compose logs -f
```

### 8. 配置 Nginx（可选）

```nginx
upstream wechat-mp-connector {
    server localhost:23014;
}

server {
    listen 80;
    server_name wechat-mp.yourdomain.com;

    location / {
        proxy_pass http://wechat-mp-connector;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 微信公众平台配置

### 1. 创建微信服务号

1. 访问 [微信公众平台](https://mp.weixin.qq.com/)
2. 注册并登录服务号
3. 获取 AppID 和 AppSecret

### 2. 配置服务器地址

1. 在微信公众平台进入"开发"->"基本配置"
2. 设置服务器地址（URL）：`https://your-domain.com/wechat-mp/webhook`
3. 设置 Token 和 EncodingAESKey
4. 启用消息加密模式（推荐）

### 3. 配置 IP 白名单

1. 在微信公众平台进入"开发"->"基本配置"
2. 添加服务器 IP 地址到白名单

### 4. 配置菜单

通过 API 创建自定义菜单：

```bash
curl -X POST http://localhost:23014/wechat-mp/menu/create \
  -H "Content-Type: application/json" \
  -d '{
    "accountId": 1,
    "menuData": {
      "button": [
        {
          "type": "click",
          "name": "今日歌曲",
          "key": "V1001_TODAY_MUSIC"
        }
      ]
    }
  }'
```

## API 文档

启动应用后，访问 Swagger UI 文档：

```
http://localhost:23014/swagger-ui.html
```

## 健康检查

```bash
curl http://localhost:23014/actuator/health
```

返回示例：

```json
{
  "status": "UP",
  "components": {
    "diskSpace": {
      "status": "UP",
      "total": 500000000000,
      "free": 250000000000,
      "threshold": 10485760
    },
    "ping": {
      "status": "UP"
    }
  }
}
```

## 故障排查

### 1. 检查日志

```bash
# Docker 日志
docker-compose logs -f

# 应用日志
tail -f logs/livepulse-connector-wechat-mp-open.log
```

### 2. 常见问题

#### 问题 1：无法连接到 PostgreSQL

检查数据库连接配置：

```bash
psql -U wechat_user -d livepulse_wechat_mp -h postgresql-host
```

#### 问题 2：无法注册到 Nacos

检查 Nacos 地址配置：

```bash
curl http://nacos-host:8848/nacos/v1/ns/instance/list?serviceName=wechat-mp-open-connector-server
```

#### 问题 3：微信 API 调用失败

检查 API 凭据配置：
- 确认 AppID 和 AppSecret 正确
- 确认 Access Token 有效
- 确认服务器地址配置正确
- 确认 IP 白名单已配置

#### 问题 4：Webhook 验证失败

检查 Webhook 配置：
- 确认 Token 配置正确
- 确认 EncodingAESKey 配置正确
- 确认服务器 URL 可访问
- 检查服务器日志获取详细错误信息

## 监控指标

### Spring Boot Actuator

访问监控端点：

```bash
# 健康检查
curl http://localhost:23014/actuator/health

# 应用信息
curl http://localhost:23014/actuator/info

# 指标数据
curl http://localhost:23014/actuator/metrics

# Prometheus 指标
curl http://localhost:23014/actuator/prometheus
```

## 数据模型说明

### 核心表结构

1. **wechat_mp_account**: 微信服务号账户配置
2. **wechat_mp_user**: 用户数据
3. **wechat_mp_material**: 素材数据
4. **wechat_mp_message**: 消息数据
5. **wechat_mp_tag**: 标签数据
6. **wechat_mp_menu**: 菜单数据
7. **wechat_mp_news**: 图文消息数据
8. **wechat_mp_webhook_log**: Webhook 日志
9. **wechat_mp_sync_log**: 同步日志

### 同步频率

- 用户：每4小时同步最近30天数据
- 素材：每天凌晨2点同步最近30天数据
- 消息：每6小时同步最近30天数据

## 安全建议

1. **敏感数据加密**：对 AppSecret 进行加密存储
2. **访问控制**：配置 Nginx IP 白名单
3. **HTTPS 配置**：生产环境使用 SSL 证书
4. **日志脱敏**：避免在日志中输出敏感信息
5. **令牌管理**：定期更新 Access Token
6. **消息加密**：启用微信消息加密模式

## 微信 API 限制

### 接口调用频率限制

- 获取 Access Token：2000次/天
- 获取用户信息：100000次/天
- 自定义菜单：100000次/天
- 素材管理：100000次/天

### 数据容量限制

- 素材文件：不超过 10MB
- 图文消息：不超过 10 条
- 自定义菜单：不超过 3 级

## 版本更新

### v2.0 (2025-05-04)

- 初始版本发布
- 支持用户数据同步
- 支持素材数据同步
- 支持消息数据同步
- 支持定时同步任务
- 支持 Webhook 事件订阅

## 技术支持

- **文档**: https://developers.weixin.qq.com/doc/
- **GitHub**: https://github.com/Wechat-Group/WxJava
- **邮箱**: support@livepulse.com.cn
